environment:
  name: "Meta Mega Codex"
  timezone: "UTC"
  data_paths:
    vault: "PreservationVault"
    stacks: "stacks"
    config: "config"
  logging:
    level: "INFO"
    fmt: "%(asctime)s %(levelname)s %(name)s — %(message)s"
